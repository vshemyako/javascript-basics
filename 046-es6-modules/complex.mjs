// only works with '--experimental-modules' nodejs flag
// Both files have '.mjs' extension!
import { firstNumber, secondNumber } from "./simple.mjs"

console.log(firstNumber);
console.log(secondNumber);